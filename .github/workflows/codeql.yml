name: "CodeQL"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '31 10 * * 0'

# Declare default permissions as read-only.
permissions: read-all

jobs:
  CodeQL:
    uses: bytemare/actions/.github/workflows/codeql.yml@ac373b63aa8863380de843ddebfd42baabf57703
    with:
      language: go
