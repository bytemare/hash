name: "CodeQL"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '31 10 * * 0'

jobs:
  CodeQL:
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false

    uses: bytemare/actions/.github/workflows/codeql.yml@fbcba37214781155710d371293998c2a129853b2
    with:
      language: go
